
import {useGetJobByIdQuery,useGetCourseByIdQuery} from '../services/jobServices/apiJobServices'



const job = {
    useGetJobByIdQuery
}

const course = {
    useGetCourseByIdQuery
}

export const apiEndpoints = {
    job,course
}